[package]
name = "rgis-ui-events"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
rgis-primitives = { path = "../rgis-primitives" }
geo-features = { path = "../geo-features" }
