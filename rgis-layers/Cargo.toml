[package]
name = "rgis-layers"
version.workspace = true
authors.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
bevy = { workspace = true }
colorous = { workspace = true }
geo-features = { path = "../geo-features", version = "0.1.0" }
geo-geom-type = { path = "../geo-geom-type", version = "0.1.0" }
geo-projected = { path = "../geo-projected", version = "0.1.0" }
rgis-events = { path = "../rgis-events", version = "0.1.0" }
rgis-primitives = { path = "../rgis-primitives", version = "0.1.0" }
geo = { workspace = true }
rgis-crs = { path = "../rgis-crs", version = "0.1.0" }
