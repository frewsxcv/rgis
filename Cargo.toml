[workspace]
members = [
    "geo-bevy",
    "geo-srs",
    "rgis",
    "rgis-camera",
    "rgis-cli",
    "rgis-file-loader",
    "rgis-keyboard",
    "rgis-layers",
    "rgis-mouse",
    "rgis-renderer",
    "rgis-ui",
    "time-logger",
]
resolver = "2"

[profile.release]
debug = true

[patch.crates-io]
# geo = { git = "https://github.com/georust/geo.git", branch = "master" }
# geo = { path = "../geo/geo" }
geo = { git = "https://github.com/georust/geo.git", branch = "mkirk/transform-redux" }
# geo-types = { git = "https://github.com/georust/geo.git", branch = "master" }
# geo-types = { path = "../geo/geo-types" }
geo-types = { git = "https://github.com/georust/geo.git", branch = "mkirk/transform-redux" }
# proj = { path = "../proj" }
proj = { git = "https://github.com/georust/proj", branch = "mkirk/transform" }
