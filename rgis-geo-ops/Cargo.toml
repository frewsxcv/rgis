[package]
name = "rgis-geo-ops"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bevy_egui = "0.20"
geo = "0.25"
geo-features = { path = "../geo-features" }
geo-geom-type = { path = "../geo-geom-type" }
geo-projected = { path = "../geo-projected" }
