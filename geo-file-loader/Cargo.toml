[package]
name = "geo-file-loader"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bytes = "1"
geo = "0.30"
geozero = { git = "https://github.com/georust/geozero.git", branch = "main", features = [
    "with-wkt",
    "with-gpx",
    "with-shp",
] }
